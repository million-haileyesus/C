#include <stdio.h>
#include <stdbool.h>

void main() {
	printf("%s", "choose a number:\n");
	int c = getchar();
	printf("%d", c);
}

void run() {
	/**
	 * int[num]* board;
	 * bool can_quit = false;
	 * for(int i = 1; i < board.length; i++) {
	 * 		board[i] = -1;
	 * 	}
	 * 	if(board.length > 0) {
	 * 		board[0] = 0;
	 * 	}
	 * 	for(int i = 0; i < board.length && !can_quit; i++) {
	 *		if(is_safe()) {
	 *		}
	 *		else {
	 *			while(
	 */
bool is_safe() {
	int k = 1;
	bool isSafe = true;
	for(int i = 0; i < 10; i++) {
		
	}
	return isSafe;
}
